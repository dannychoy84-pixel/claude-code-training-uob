# Training brief contains personal contact details (phone/email) — keep local only
Training prompt.txt

# Local machine-specific Claude Code settings
.claude/settings.local.json
.claude/scheduled_tasks.lock

# OS/editor cruft
.DS_Store
Thumbs.db

# Installed Claude Code skills — reproducible via the commands in README.md,
# not committed (large third-party data files + Windows-only symlinks)
.agents/skills/
.claude/skills/
